
Promotion failed
Your deployment attempt had the following errors:
Missing DATABASE_URL environment variable in the deployment
The application is crashing due to database initialization failure
Missing required secret variable for database connection
Assistant suggestions
Add the DATABASE_URL secret to your deployment configuration
if (!process.env.DATABASE_URL) {
  throw new Error(
    "DATABASE_URL must be set. Did you forget to provision a database?",
  );
}
if (!process.env.DATABASE_URL) {
  console.warn(
    "DATABASE_URL is not set. Some features requiring database access will not function."
  );
  // Provide a fallback or mock implementation
}
// Initialize the database
  try {
    // Check if database connection is working
    const isConnected = await checkDatabaseConnection();
    
    if (isConnected) {
      log('Successfully connected to PostgreSQL database');
      // Initialize database (create tables if needed)
      await initDatabase();
      log('Database initialized successfully');
    } else {
      log('Error connecting to PostgreSQL database - using fallback storage');
    }
  } catch (error: any) {
    log(`Database error: ${error.message}`);
  }
// Initialize the database
  try {
    // Check if database connection is working
    if (process.env.DATABASE_URL) {
      const isConnected = await checkDatabaseConnection();
      
      if (isConnected) {
        log('Successfully connected to PostgreSQL database');
        // Initialize database (create tables if needed)
        await initDatabase();
        log('Database initialized successfully');
      } else {
        log('Error connecting to PostgreSQL database - using fallback storage');
      }
    } else {
      log('DATABASE_URL not set - skipping database initialization');
    }
  } catch (error: any) {
    log(`Database error: ${error.message}`);
  }
- production secret variable names: SESSION_SECRET, OPENAI_API_KEY
- production secret variable names: SESSION_SECRET, OPENAI_API_KEY, DATABASE_URL